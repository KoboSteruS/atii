# 🔄 Альтернативные способы синхронизации данных

## Вариант 1: GitHub API (рекомендую)

### Как работает:
- JSON файл хранится в GitHub репозитории
- При изменении данных → сохраняется в GitHub через API
- При загрузке → читается из GitHub
- Все видят актуальные данные

### Плюсы:
✅ Бесплатно  
✅ Версионирование (видно историю изменений)  
✅ Не нужен отдельный сервис  
✅ Работает из коробки

### Минусы:
❌ Нужен GitHub токен  
❌ Лимиты API (5000 запросов/час - достаточно)

### Настройка:
1. Создать GitHub репозиторий
2. Создать Personal Access Token
3. Добавить в `.env`:
```env
VITE_GITHUB_TOKEN=your-token
VITE_GITHUB_REPO=username/repo-name
VITE_GITHUB_FILE=data.json
```

---

## Вариант 2: JSON файл на GitHub Pages

### Как работает:
- JSON файл лежит в репозитории GitHub
- При загрузке → читается с GitHub Pages
- Обновление: вручную коммитить файл или через GitHub API

### Плюсы:
✅ Очень просто  
✅ Не нужна регистрация  
✅ Бесплатно

### Минусы:
❌ Только чтение (или ручное обновление)  
❌ Нет реального времени

---

## Вариант 3: Простой Node.js сервер

### Как работает:
- Минимальный Express сервер
- Хранит JSON файл на сервере
- REST API для чтения/записи

### Плюсы:
✅ Полный контроль  
✅ Можно на любом хостинге  
✅ Простая настройка

### Минусы:
❌ Нужен сервер (VPS/хостинг)  
❌ Нужно поддерживать

### Код сервера (~50 строк):
```javascript
const express = require('express');
const fs = require('fs');
const app = express();

app.use(express.json());

app.get('/api/data', (req, res) => {
  const data = JSON.parse(fs.readFileSync('data.json'));
  res.json(data);
});

app.post('/api/data', (req, res) => {
  fs.writeFileSync('data.json', JSON.stringify(req.body, null, 2));
  res.json({ success: true });
});

app.listen(3000);
```

---

## Вариант 4: Google Sheets API

### Как работает:
- Данные в Google таблице
- Чтение/запись через API

### Плюсы:
✅ Удобно редактировать в таблице  
✅ Бесплатно

### Минусы:
❌ Нужна регистрация Google Cloud  
❌ Сложнее настройка

---

## Рекомендация

**Для начала:** Вариант 1 (GitHub API) - самый простой и надежный  
**Если нет GitHub:** Вариант 3 (Node.js сервер) - полный контроль

Какой вариант выбираем?
