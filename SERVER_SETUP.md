# 🖥️ Настройка сервера синхронизации данных

Простой Node.js сервер для хранения данных в JSON файле.

---

## 📋 Шаг 1: Установка зависимостей

```bash
cd server
npm install
```

---

## 📋 Шаг 2: Запуск сервера

```bash
npm start
```

Сервер запустится на `http://localhost:3001`

Для разработки с автоперезагрузкой:
```bash
npm run dev
```

---

## 📋 Шаг 3: Настройка фронтенда (опционально)

Если сервер на другом порту или адресе, создайте файл `.env` в корне проекта:

```env
VITE_API_URL=http://localhost:3001
```

Или для production:
```env
VITE_API_URL=https://your-server.com
```

---

## 📋 Шаг 4: Проверка работы

1. Запустите сервер: `cd server && npm start`
2. Запустите фронтенд: `npm run dev`
3. Откройте админку: `http://localhost:5173/admin`
4. Внесите изменения - они автоматически сохранятся на сервер
5. Откройте сайт в другом браузере - изменения появятся автоматически!

---

## 📁 Структура

```
server/
  ├── index.js          # Код сервера
  ├── package.json      # Зависимости
  ├── data.json         # Файл с данными (создается автоматически)
  └── README.md         # Документация
```

---

## 🔄 Как это работает

1. **При сохранении в админке:**
   - Данные сохраняются в `localStorage` (для быстрого доступа)
   - Данные отправляются на сервер → сохраняются в `server/data.json`

2. **При загрузке:**
   - Сначала пытается загрузить с сервера
   - Если сервер недоступен → использует `localStorage`
   - Если и localStorage пуст → использует дефолтные данные

3. **Автоматическая синхронизация:**
   - Каждые 30 секунд проверяет обновления на сервере
   - Кнопка "Обновить" в админке загружает данные вручную

---

## 🚀 Деплой на сервер

### Вариант 1: VPS/хостинг

1. Загрузите папку `server/` на сервер
2. Установите Node.js на сервере
3. Запустите:
```bash
cd server
npm install
npm start
```

Для постоянной работы используйте `pm2`:
```bash
npm install -g pm2
pm2 start server/index.js --name atii-sync
pm2 save
pm2 startup
```

### Вариант 2: Heroku/Railway/Render

1. Создайте аккаунт на платформе
2. Подключите GitHub репозиторий
3. Укажите команду запуска: `cd server && npm start`
4. Укажите порт: `PORT` (платформа сама назначит)

---

## 🛠️ Troubleshooting

### Сервер не запускается
- Проверьте, что Node.js установлен: `node --version`
- Проверьте, что порт 3001 свободен
- Измените порт через переменную окружения: `PORT=3002 npm start`

### Данные не синхронизируются
- Убедитесь, что сервер запущен
- Проверьте консоль браузера (F12) на ошибки
- Проверьте, что `VITE_API_URL` правильный в `.env`

### Ошибка CORS
- Сервер уже настроен с `cors()` - должно работать
- Если проблема остается, проверьте настройки браузера

---

## 📝 API Endpoints

- `GET /api/data` - получить все данные
- `POST /api/data` - сохранить все данные
- `GET /api/data/:key` - получить конкретный ключ (websites, templates, pages, settings, workflowSchemas)
- `POST /api/data/:key` - сохранить конкретный ключ

---

## 💡 Преимущества

✅ Простота - всего ~100 строк кода  
✅ Локальное хранение - данные в JSON файле  
✅ Без внешних зависимостей - только Express  
✅ Легко настроить и поддерживать  
✅ Работает офлайн - fallback на localStorage  

---

Готово! Теперь все изменения синхронизируются через ваш сервер! 🎉
