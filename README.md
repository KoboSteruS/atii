# АТИИ — Сайт IT-компании

Современный многостраничный сайт IT-компании с полноценной админ-панелью и визуальным редактором workflow.

## 🚀 Возможности

### Публичная часть:
- **Главная** — Hero, Features, Projects, Solutions, Capabilities, Stats
- **О нас** — Hero, Stats, Values, Technologies
- **Готовые решения** — Шаблоны с интерактивными workflow
- **Под ключ** — Workflow процесса, Advantages, Case Studies
- **Портфолио** — Проекты с фильтрацией и поиском

### Админ-панель:
- **Dashboard** — Статистика и активность
- **Workflow Editor** — Визуальный редактор схем в стиле n8n
- **Content Management** — Редактирование всех страниц
- **Templates** — Управление шаблонами решений
- **Portfolio** — Управление проектами
- **Settings** — Настройки сайта
- **Экспорт/Импорт** — Резервное копирование данных
- **Импорт из draw.io** — Загрузка схем из draw.io

---

## 📦 Установка

### Требования:
- Node.js 18+ 
- npm 9+

### Шаги:

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd ATII
```

2. **Установите зависимости:**
```bash
npm install
```

3. **Запустите dev-сервер:**
```bash
npm run dev
```

4. **Откройте в браузере:**
```
http://localhost:3000
```

---

## 🛠️ Команды

```bash
# Разработка
npm run dev          # Запуск dev-сервера (localhost:3000)

# Сборка
npm run build        # Production сборка
npm run preview      # Предпросмотр production сборки

# Линтинг
npm run lint         # Проверка кода
```

**Production:** при деплое за nginx API по умолчанию вызывается по относительному пути `/api/v1`. Если бэкенд на другом домене — собери с переменной: `VITE_API_URL=https://api.example.com/api/v1 npm run build`.

---

## 📁 Структура проекта

```
ATII/
├── src/
│   ├── components/
│   │   ├── Home.tsx              # Главная страница
│   │   ├── About.tsx             # О нас
│   │   ├── Templates.tsx         # Готовые решения
│   │   ├── CustomSolutions.tsx   # Под ключ
│   │   ├── Portfolio.tsx         # Портфолио
│   │   ├── AdminPanel.tsx        # Админ-панель
│   │   ├── WorkflowEditor.tsx    # Визуальный редактор схем
│   │   ├── Footer.tsx            # Подвал
│   │   └── ...
│   ├── store/
│   │   └── AppContext.tsx        # Глобальное состояние
│   ├── assets/                   # Изображения, иконки
│   ├── App.tsx                   # Главный компонент
│   └── main.tsx                  # Точка входа
├── public/                       # Статические файлы
├── ADMIN_GUIDE.md               # Руководство по админке
├── WORKFLOW_EDITOR_GUIDE.md     # Руководство по редактору
├── package.json
├── vite.config.ts
└── README.md
```

---

## 🎨 Технологии

### Frontend:
- **React 19** — UI библиотека
- **TypeScript** — Типизация
- **Vite** — Сборщик
- **Tailwind CSS** — Стили
- **Motion (Framer Motion)** — Анимации
- **Lucide React** — Иконки
- **React Router** — Маршрутизация

### State Management:
- **Context API** — Глобальное состояние
- **localStorage** — Персистентность данных

### Дополнительно:
- **XML Parser** — Импорт из draw.io
- **JSON Export/Import** — Резервное копирование

---

## 🔑 Доступ к админке

### URL:
```
http://localhost:3000/admin
```

### Или:
Нажмите кнопку **"Admin"** в навигации

### Данные для входа:
Не требуются (пока нет авторизации)

---

## 📚 Документация

### Руководства:
- [Руководство по админ-панели](./ADMIN_GUIDE.md)
- [Руководство по Workflow Editor](./WORKFLOW_EDITOR_GUIDE.md)

### Основные функции:

#### 1. Редактирование контента:
```
Admin → Content → Выбрать страницу → Редактировать
```

#### 2. Создание проекта в портфолио:
```
Admin → Portfolio → Добавить проект → Заполнить форму → Создать
```

#### 3. Создание workflow схемы:
```
Admin → Schemas → Выбрать шаблон → Добавить узлы → Сохранить
```

#### 4. Импорт схемы из draw.io:
```
Admin → Schemas → Импорт draw.io → Выбрать файл
```

#### 5. Экспорт данных:
```
Admin → Кнопка "Экспорт" → Скачать JSON
```

---

## 🎯 Использование

### Для разработчиков:

1. **Локальная разработка:**
```bash
npm run dev
```

2. **Изменение контента:**
- Откройте админку
- Отредактируйте нужный раздел
- Сохраните изменения
- Изменения сразу отобразятся на сайте

3. **Добавление новых страниц:**
- Создайте компонент в `src/components/`
- Добавьте маршрут в `src/App.tsx`
- Добавьте страницу в `AppContext.tsx` (если нужно редактирование)
- Добавьте редактор в `AdminPanel.tsx`

### Для контент-менеджеров:

1. **Редактирование текстов:**
```
Admin → Content → Главная → Редактировать → Изменить Hero/Features/etc → Сохранить
```

2. **Добавление проектов:**
```
Admin → Portfolio → Добавить проект → Заполнить → Создать
```

3. **Создание шаблонов:**
```
Admin → Templates → Добавить шаблон → Заполнить → Создать
Admin → Schemas → Выбрать шаблон → Создать схему → Сохранить
```

---

## 🔄 Синхронизация данных

### Автоматическая синхронизация:
- Все изменения в админке сохраняются в `localStorage`
- Изменения сразу отображаются на сайте
- Работает между вкладками браузера
- Использует `storage` events + custom events

### Ключи localStorage:
```javascript
atii_websites          // Портфолио
atii_templates         // Шаблоны
atii_pages             // Контент страниц
atii_settings          // Настройки
atii_workflow_schemas  // Схемы workflow
```

### Резервное копирование:
```
Admin → Экспорт → Скачать JSON → Сохранить файл
```

### Восстановление:
```
Admin → Импорт → Выбрать JSON → Подтвердить → Перезагрузка
```

---

## 🚀 Деплой

### Production сборка:
```bash
npm run build
```

Результат в папке `build/`

### Деплой на Vercel:
```bash
vercel --prod
```

### Деплой на Netlify:
```bash
netlify deploy --prod --dir=build
```

### Деплой на собственный сервер:
```bash
npm run build
scp -r build/* user@server:/var/www/atii/
```

---

## 🔧 Конфигурация

### Vite (vite.config.ts):
```typescript
{
  server: {
    host: '0.0.0.0',  // Доступ через VPN
    port: 3000,
    open: false
  }
}
```

### Tailwind (tailwind.config.js):
- Кастомные цвета
- Анимации
- Утилиты

---

## 🐛 Troubleshooting

### Проблема: Порт 3000 занят
**Решение:**
```bash
# Изменить порт в vite.config.ts
server: { port: 3001 }
```

### Проблема: Данные не сохраняются
**Решение:**
- Проверьте localStorage в DevTools
- Очистите кэш браузера
- Проверьте консоль на ошибки

### Проблема: Импорт draw.io не работает
**Решение:**
- Используйте формат .drawio или .xml
- Убедитесь, что в схеме есть блоки с текстом
- Проверьте консоль на ошибки парсинга

### Проблема: Белый экран
**Решение:**
```bash
# Очистить node_modules и переустановить
rm -rf node_modules package-lock.json
npm install
npm run dev
```

---

## 📈 Roadmap

### v2.0 (Текущая):
- ✅ Полноценная админка
- ✅ Workflow Editor в стиле n8n
- ✅ Импорт из draw.io
- ✅ Экспорт/Импорт данных
- ✅ Синхронизация в реальном времени

### v2.1 (Планируется):
- [ ] Авторизация в админке
- [ ] Роли и права доступа
- [ ] Медиа-библиотека (загрузка изображений)
- [ ] История изменений
- [ ] Предпросмотр перед публикацией

### v2.2 (Планируется):
- [ ] Backend (Node.js + Express)
- [ ] База данных (PostgreSQL)
- [ ] API для управления контентом
- [ ] Многоязычность (i18n)
- [ ] SEO-оптимизация

### v3.0 (Будущее):
- [ ] CMS как отдельный продукт
- [ ] Плагины и расширения
- [ ] Конструктор страниц (drag & drop)
- [ ] A/B тестирование
- [ ] Аналитика и метрики

---

## 🤝 Вклад

Проект разработан для внутреннего использования. Предложения и улучшения приветствуются.

---

## 📄 Лицензия

Proprietary — Все права защищены © 2026 АТИИ

---

## 👥 Команда

- **Разработка:** AI Assistant + Developer
- **Дизайн:** Figma Export + Custom
- **Контент:** АТИИ Team

---

## 📞 Контакты

- **Email:** admin@atii.ru
- **Телефон:** +7 (XXX) XXX-XX-XX
- **Адрес:** Москва, Россия

---

## 🎉 Благодарности

- **React Team** — за отличный фреймворк
- **Tailwind CSS** — за удобные утилиты
- **Framer Motion** — за плавные анимации
- **Lucide** — за красивые иконки
- **Vite** — за быструю сборку

---

**Версия:** 2.0  
**Последнее обновление:** 2026-01-18  
**Статус:** ✅ Production Ready
