# 🔥 Настройка Firebase для синхронизации данных

## Проблема
Без Firebase изменения в админке сохраняются только в `localStorage` вашего браузера. Коллеги не видят ваши изменения.

## Решение
Firebase Realtime Database синхронизирует данные между всеми пользователями в реальном времени.

---

## 📋 Шаг 1: Создание проекта Firebase

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Нажмите **"Add project"** (Добавить проект)
3. Введите название проекта (например: `atii-website`)
4. Отключите Google Analytics (не обязательно)
5. Нажмите **"Create project"**

---

## 📋 Шаг 2: Создание Realtime Database

1. В меню слева выберите **"Realtime Database"**
2. Нажмите **"Create Database"**
3. Выберите регион (например: `europe-west1` для России)
4. Выберите режим: **"Start in test mode"** (для начала)
5. Нажмите **"Enable"**

⚠️ **Важно:** В тестовом режиме база открыта для всех. После настройки измените правила безопасности!

---

## 📋 Шаг 3: Получение конфигурации

1. В меню слева выберите **⚙️ Project Settings** (Настройки проекта)
2. Прокрутите вниз до секции **"Your apps"**
3. Нажмите на иконку **Web** (`</>`)
4. Введите название приложения (например: `ATII Website`)
5. Нажмите **"Register app"**
6. Скопируйте конфигурацию (она выглядит так):

```javascript
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "your-project.firebaseapp.com",
  databaseURL: "https://your-project-default-rtdb.firebaseio.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef"
};
```

---

## 📋 Шаг 4: Настройка переменных окружения

1. Создайте файл `.env` в корне проекта (рядом с `package.json`)
2. Добавьте следующие строки, заменив значения на свои из Firebase:

```env
VITE_FIREBASE_API_KEY=AIzaSy...
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_DATABASE_URL=https://your-project-default-rtdb.firebaseio.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abcdef
```

3. Сохраните файл

---

## 📋 Шаг 5: Настройка правил безопасности (важно!)

1. В Firebase Console перейдите в **Realtime Database**
2. Откройте вкладку **"Rules"**
3. Замените правила на следующие:

```json
{
  "rules": {
    "atii": {
      ".read": true,
      ".write": true
    }
  }
}
```

⚠️ **Внимание:** Эти правила открывают доступ для всех. Для production используйте аутентификацию!

4. Нажмите **"Publish"**

---

## 📋 Шаг 6: Перезапуск приложения

1. Остановите dev-сервер (Ctrl+C)
2. Запустите снова:

```bash
npm run dev
```

---

## ✅ Проверка работы

1. Откройте админку: `http://localhost:5173/admin`
2. Внесите любое изменение (например, измените текст на главной странице)
3. Откройте сайт в другом браузере (или на другом компьютере)
4. Изменения должны появиться автоматически!

---

## 🔄 Как это работает

- **При сохранении:** Данные сохраняются в Firebase и `localStorage`
- **При загрузке:** Данные загружаются из Firebase (если настроен) или из `localStorage`
- **В реальном времени:** Все пользователи видят изменения сразу

---

## 🛠️ Troubleshooting

### Firebase не подключается
- Проверьте, что все переменные в `.env` заполнены правильно
- Убедитесь, что файл называется именно `.env` (не `.env.local` или другое)
- Перезапустите dev-сервер после создания `.env`

### Данные не синхронизируются
- Проверьте правила безопасности в Firebase Console
- Убедитесь, что `databaseURL` правильный
- Откройте консоль браузера (F12) и проверьте ошибки

### Ошибка "Permission denied"
- Проверьте правила безопасности в Firebase Console
- Убедитесь, что правила опубликованы (кнопка "Publish")

---

## 💰 Стоимость

Firebase Realtime Database имеет **бесплатный тариф** (Spark plan):
- 1 GB хранения
- 10 GB трафика в месяц
- 100 одновременных подключений

Этого достаточно для небольшого сайта. Если нужно больше - есть платные тарифы.

---

## 🔒 Безопасность (для production)

Для production сайта настройте аутентификацию:

1. В Firebase Console → Authentication → Enable
2. Измените правила базы данных:

```json
{
  "rules": {
    "atii": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}
```

3. Добавьте авторизацию в приложение (это отдельная задача)

---

Готово! Теперь все изменения синхронизируются между пользователями! 🎉
